# Website task board

Current phase: LOCAL BUILD authorized (Evan, 2026-09-10, coordinator-prompt.md handoff). This supersedes the earlier preparation-only instruction. Authorized: design, writing, dependency installation, implementation, local servers, agent reviews, and fixes. Still NOT authorized: deploying, pushing, committing, changing remote Pages settings, contacting anyone, or Canvas submission.

Stack: Next.js App Router + TypeScript + CSS/CSS Modules, with static export for GitHub Pages. This replaces the original plain-HTML/no-build plan.

A later local-preview instruction can authorize provisional copy and implementation without completing H01 publication approval. Record that limited authorization under P00/P02 and keep H01 and deployment tasks pending.

Goals in order: (1) finish all course deliverables by the Canvas deadline (README currently says September 22, 2026), (2) build a credible founder home base. Launch scope: compact homepage and three reviewed articles. Mostly black/white with optional restrained brown/navy accents.

## How agents pick up work

Read AGENT-WORKFLOW.md and your role file. Request a ready task for your role; coordinator reserves it and records assignee/run, status, and write scope before dispatch. Tasks are ready only when dependencies and phase authorization are met. A checkbox means acceptance evidence was inspected, not merely that an agent returned.

For each active task the coordinator adds: `Status: ... | Assignee/run: ... | Write scope: ... | Report: ...`. Reports use `reports/<ID>-<role>-<round>.md`. Mark blocked tasks with missing input and next action. Keep shared board updates serialized through the coordinator.

## Team setup

- [x] S04 | Coordinator | Update stack plan, rules, builder roles, task board, and local coordinator prompt for Next.js static export. Evidence: website-plan.md, CLAUDE.md, AGENT-WORKFLOW.md, coordinator-prompt.md, reports/S04-coordinator-01.md. Documentation only; no installation or implementation performed.

- [x] S01 | Coordinator | Define three builder roles and four audience-review roles in .claude/agents/. Acceptance: seven distinct role files with goals, scope, handoffs, and report requirements. Evidence: role files; reports/S01-S03-coordinator-01.md.
- [x] S02 | Coordinator | Define workflow, safe parallel ownership, report template, independent review procedure, and decision-log integration. Evidence: AGENT-WORKFLOW.md, AGENTS.md, reports/README.md, reports/TEMPLATE.md; reports/S01-S03-coordinator-01.md.
- [x] S03 | Coordinator | Create dependency-aware checklist and connect it to existing project rules and plan. Evidence: this board and linked shared files; reports/S01-S03-coordinator-01.md. Only setup is complete.

## Preparation for the build

- [x] M01 | Coding / motion-01 | Complete. Evidence: reports/M01-coding-01.md and reports/M01-coordinator-01.md. Build, typecheck, lint, dev/static browser checks and accessibility passed. Evan authorized implementing the motion note on 2026-09-22. Scope: app/globals.css, app/page.tsx, components/Section.tsx, components/Section.module.css, link component CSS if needed, reports/M01-coding-01.md. Implement 180ms navy underline draw and selective 8px reveals, instant reduced-motion and readable no-JS/focus paths. Local implementation and checks only.

- [x] P-MOTION | Coordinator | Record Evan's supplied restrained-motion design note (2026-09-22). Documentation only; implementation remains deferred for this request. Evidence: website-plan.md section 5; reports/P-MOTION-coordinator-01.md.

- [x] P00 | Coordinator | Record Evan's authorization for the next phase and its scope, then activate ready tasks. Dependency: S01-S03. Evidence: Evan's 2026-09-10 local-build instruction (coordinator-prompt.md text delivered in session), recorded in this board header, website-plan.md status, and reports/P00-P02-coordinator-01.md. Scope: local build only. Provisional copy authorized for the LOCAL preview; H01 byline approval and all deployment/submission tasks stay pending.
- [x] C01 | Coding | Establish repo/remote/deployment-path facts and check plan without publishing. Evidence: reports/C01-coding-01.md. Repo is evanbaker315/personalWebsite, so a Pages project site needs basePath /personalWebsite; both the empty and prefixed builds were exported and every _next asset fetched at 200. Node 20.13.1 with next 16.3.4 / react 19.3.0 / marked 18.0.12 (4 runtime deps). Scripts dev/build/typecheck/lint/preview/build:pages defined. Check plan recorded. Coordinator independently verified: AGENTS.md restored to its original 11 lines after Next 16's dev server injected a managed block (agentRules:false now prevents it), .gitignore preserves context/* and writing/* verbatim, and git add -A --dry-run stages no private or build file.
- [x] W01 | Writer | Draft compact homepage copy and private claim map. Evidence: context/homepage-copy.md (~719 rendered words, block-delimited for implementation), context/claim-review.md (71 mapped claims), reports/W01-W02-writer-01.md. Coordinator inspected the copy: product usefulness leads both company entries, Evan's contribution is stated for both companies and explicitly bounded for team-built professional work, one supported commercial decision per company (Demarly BYO-key/per-agent budgets; EdgeBet tools-not-picks), thesis appears once in About, no invented metrics. Unresolved inputs are marked as tokens, not guesses.
- [x] W02 | Writer | Correct the three launch articles. Evidence: revised writing/the-30x-bug.md, writing/distrust-your-best-results.md, writing/agents-that-cant-lie.md; reports/W01-W02-writer-01.md. Coordinator read all three revisions. Editorial queue addressed: pre-launch/pre-billing moved to paragraph two of the 30x bug; the unmeasured "almost never" frequency claim in distrust-your-best-results replaced with the encoded prior plus an explicit statement that no backtest exists, and the conservative rerun correctly scoped to player props; the approval piece retitled to "Draft anything, send nothing" with the truthfulness guarantee downgraded to a mitigation and a "where the guarantee stops" section added. No expansion into the seven backlog drafts.
- [x] D01 | Design | Prepare visual specification from real content and approved palette. Evidence: reports/design-spec.md (617 lines), reports/D01-design-01.md. Concrete tokens, type scale, spacing, desktop/375px/200%-zoom layouts, focus states, and one signature choice (the method line: a hand-authored SVG dimension line in a single full-bleed black band). Values were measured from a rendered scratchpad prototype in headless Chrome, not estimated. Coordinator independently recomputed the WCAG contrast for #4D4D4D (8.45:1), #1F3352 (12.70:1), #6B4A2F (7.94:1), and #B8B8B8 on black (10.59:1); all matched the spec. Lowest shipping ratio is 6.16:1. Accents are structurally barred from the black band, so no dark brown or navy small text can land on black. Limitation: a specification is not an implementation; D02 must inspect the real build.
- [ ] H01 | Evan, coordinated by writer | Review homepage claims and exact revised article text; resolve personal factual inputs. Dependency: W01, W02. Acceptance: dated byline approval for each selected revision and clear disposition of unverified claims. Existing launch/contact confirmations stay valid within their stated scope.
- [x] P01 | Coordinator with design/writer/coding | Accept implementation handoffs and record accepted design choices. Evidence: reports/P01-coordinator-01.md. Accepted on the LOCAL-PREVIEW path (W01/W02 provisional revisions) rather than the publication path; H01 stays open. Ten coordinator decisions recorded and issued to coding in the C03/C04 packet: palette polarity, the bold move, App Store URL, LinkedIn dropped, dates omitted, article retitle and slug, /writing/ index kept, scaffold article deleted, root starter files left alone, favicon added. Dependency: C01, D01, and H01 for publication; an explicitly authorized local preview may use W01/W02 provisional revisions instead. Acceptance: public copy revisions, visual spec, link targets, article publication approach, and any remaining optional items are explicit. Preserve DECISIONS draft markers.

## Implementation

- [x] P02 | Coordinator | Record authorization to implement if not already included in P00. Dependency: P01. Evidence: the same 2026-09-10 instruction covers implementation, dependency installation, and local testing; recorded in reports/P00-P02-coordinator-01.md. Implementation is active. Publication (C02, C06, C07) remains unauthorized.
- [ ] C02 | Coding | Prove the deploy pipeline early when deployment is authorized. Dependency: C01, P02 and publication authorization. Acceptance: Next.js out/ artifact deployed through Pages Actions and current page/assets fetched/viewed; report exact URL and date. If publication is not authorized yet, keep this pending and continue independent local implementation.
- [x] C03 | **Performed by the coordinator, not coding (deviation recorded)** | Set up Next.js App Router, TypeScript, npm scripts/lockfile, static-export config, and build the compact homepage with shared components/CSS. Dependency: P02. Evidence: reports/C03-C04-coordinator-01.md. Ten components with per-component CSS modules over the tokens in app/globals.css; homepage, /writing/, three article routes, 404 and icon all export. Measured in headless Chrome against the static export: h1 56/48/36px, body 19px, measure 70 chars at 1440, no horizontal scroll at 375/640/1440, zero navy or brown resolving anywhere inside the black band, zero unlabelled SVGs, exactly one method-line variant in the accessibility tree. typecheck, lint and build all clean. Ownership note: AGENT-WORKFLOW.md assigns this to coding; the coordinator built it directly, Evan caught it, and the remaining tasks were handed back to the proper agents.
- [x] C04 | **Performed by the coordinator, not coding (deviation recorded)** | Transfer the three selected article texts to tracked content/articles/ and statically generate /writing/[slug]/ pages with a shared layout. Dependency: C03; W02 plus the local-preview authorization stands in for H01, which stays open. Evidence: reports/C03-C04-coordinator-01.md. Transfer was scripted rather than retyped: the script strips only the DRAFT NOTE blocks, the H1 and the provisional date line, then asserts that no draft note and no em dash survived. Closes writer finding W-11 mechanically. Slugs are statically enumerated in ARTICLE_SLUGS; the retitled piece ships at draft-anything-send-nothing. Scaffold article deleted. Article text renders PROVISIONALLY under the local-preview authorization; H01 is not satisfied.
- [x] W03 | Writer / run 02 | Review integrated homepage/articles against approved text. Dependency: C03, C04. Evidence: reports/W03-writer-02.md. Status returned: needs-fixes (1 publication blocker, 6 suggestions, W-12 to W-18). Coordinator inspected the evidence rather than the summary. The check that earned its keep was running the copy comparison in BOTH directions: forward matching passed 42 of 45 approved blocks and would have missed the band caption entirely, because the caption is an addition rather than a modified block. Only enumerating every string the site renders and matching each back to a source surfaced it. Article bodies verified byte-identical to the reviewed drafts after the three scripted strips (16, 12 and 19 blocks per file). All five round-01 corrections survive into rendered HTML. Zero hits across the export for em dashes, draft markers, unresolved tokens, banned words, phone/address patterns, GPA, or any main.md section 53 verify-list number. Reviewed the static export only; no viewport, link, Lighthouse, keyboard or zoom checks, all correctly stated as not run.
- [x] C05 | Coding / run 02 | Ran the technical checks on the candidate and fixed FIX-001 and FIX-002. Dependency: C03, C04. Evidence: reports/C05-coding-02.md. Status returned: complete, no blockers. **Lighthouse 13.4.1 accessibility 100 on all four page types with zero flagged items, and axe-core 4.13 reports 0 violations across 8 page/width combinations.** Keyboard: the skip link is focus stop 1 on every page, Enter moves to #main, and all 20 focus stops show the navy ring at 3px offset. 200% zoom clean. Zero console messages, exceptions or HTTP 400s on any route. basePath export served under /personalWebsite/: 24 internal URLs all prefixed, zero unprefixed, 19 assets and routes all 200, then the unprefixed build restored. .nojekyll present in both builds. External links checked 2026-09-10: demarly.ai 200, github.com/evanbaker315 200, App Store listing 200. git add -A --dry-run stages 67 paths and nothing from context/, writing/, node_modules, .next or out/. Coordinator independently re-ran the overflow and heading audit against the export rather than accepting the summary: the /writing/ 1->3 skip is gone, the overflow list is empty at 375/640/1440 on every page type (it previously flagged the skip link), and the type scale is unchanged at 56/48/36px. Honest caveat the agent recorded itself: 4 Lighthouse color-contrast results are *incomplete* rather than passed, because axe cannot resolve a background through an SVG node; the band labels were computed by hand at 21:1. Original acceptance: typecheck, configured lint, npm run build producing out/, static-server preview of that output, direct article URLs/basePath/assets, console errors, keyboard, headings/alt text, mobile 375px, 200% zoom, and actual Lighthouse accessibility results documented; failures assigned and rechecked. State tool/access limitations. Already run in C03/C04 and not to be re-derived without reason: typecheck, lint, build, the clamp/measure/overflow/band-accent/SVG audit.
- [x] D02 | Design / run 02 | Inspected the desktop/mobile renders and readability. Dependency: C03, C04. Evidence: reports/D02-design-02.md. Status returned: needs-fixes, 0 blockers, 2 important, 4 suggestions (design-05 to design-10). **All five coordinator deviations accepted**, with reasoning rather than deference: the grey work-source line reads correctly quieter than a brown deck (an attribution is skippable, a deck completes a heading), the category uses brown's existing classification role, the footer flex gap holds one line down to 375, and the 64px band-to-section gap was measured and does not read as dropped. Direction survived implementation: homepage rhythm measured exact against the spec at all 24 block boundaries, the band is unambiguously the loudest element, focus is visible at every tab stop, no template tells. Renders were inspected as images at desktop, 640, mobile and 200% zoom, not read from code.
- [x] P03 | Coordinator | Resolved the builder findings and froze review candidate C1. Dependency: W03, C05, D02 and their fixes (F01, F03). Output: reports/candidate.md. Candidate C1: static export at http://localhost:4321/, source manifest 137dbb18d8810cbcf1e7029b5e8e2cbd, export manifest 4160a5a5516bf8caa281043465831407, out/index.html md5 374597ccaa64fe4dd07189066a5e0826. Built once at 17:14 with the dev server stopped; every check since ran against that one build. Coordinator re-verified the frozen artifact independently rather than accepting agent summaries: zero heading skips and zero out-of-viewport elements on all six page/width combinations, zero figcaption elements and zero hits for the deleted caption string, both `+` decks restored, four h2 in the retitled article, zero em dashes and zero draft markers anywhere in out/. **Site writes are frozen for the review round** per W-20. Known limitations are enumerated in candidate.md so reviewers do not report them as discoveries.

## Audience reviews of the finished candidate

- [x] R01 | Investor / run 01 | Assessed the public site through VC and family-office lenses. Evidence: reports/R01-investor-01.md. Status: needs-fixes, 0 blockers, 5 important, 4 suggestions. Best finding: the stated thesis is generic while the site demonstrates a sharper unstated one four times over (every system is structurally prevented from acting alone). Also caught the Demarly status undersell and the empty GitHub profile, and corroborated demarly.ai, the App Store listing and the GitHub profile with recorded URLs and access dates. Kept the two lenses separate where they disagreed about the absence of traction rather than averaging them. Limitation it stated itself: read the export with no CSS, so no visual judgment. Original: Investor reviewer | Assess public site through VC and family-office lenses. Dependency: P03. Acceptance: public first impression, linked proof checks, execution/commercial clarity/cohesion assessment, justified findings with builder owners; clearly simulated, no invented funding decision.
- [x] R02 | Boss / run 01 | Assessed professional presentation from an employer perspective. Evidence: reports/R02-boss-01.md. Status: needs-fixes, 0 blockers, 4 important, 1 suggestion. Best finding, and the catch of the round: About says "Alongside the companies I've worked professionally" while Selected work says "Before the companies, I worked on AI systems". Four other passes read both sentences and none noticed they disagree. Correctly separated what it could assess from what depends on employment terms it cannot see, recommended no resume reframing, and recorded seven strengths so a fix pass does not sand off the hedging that makes the page credible. Original: Boss reviewer | Assess professional presentation from an employer perspective. Dependency: P03. Acceptance: specific published attribution/disclosure concerns separated from unknown employment policy; preserve founder identity and voice; no speculative misconduct claims or promise of employer clearance.
- [x] R03 | Teacher / run 01 | Checked the candidate and process evidence against README. Evidence: reports/R03-teacher-01.md. Status: needs-fixes, 6 blockers, 8 important, 2 suggestions, no grade or rubric invented and no DECISIONS answer drafted. Live GETs of both candidate Pages URLs returned 404 on 2026-09-10. Deliverables: live site not met, DECISIONS partially met (Q3 and Q5 still empty), verification/ not met, video and classmate comments not met and not verifiable from here. Independently re-confirmed that the frozen export contains the F03 corrections it claims. Its closing line is the honest one: the process record is strong, and all of it certifies a local artifact. Original: Teacher reviewer | Check candidate and available process evidence against README. Dependency: P03. Acceptance: each of five deliverables classified with evidence; missing live/submission items explicitly pending; no invented grade/rubric and no Q3 ghostwriting.
- [x] R04 | Classmate / run 01 | Read the site as a newcomer. Evidence: reports/R04-classmate-01.md. Status: needs-fixes, 0 blockers, 2 important, 4 suggestions. Produced the single most valuable data point of the round: on a 60-second skim the method line **did not decode** and the reader skipped it, finding the connection between the companies only in About, which is the last section it would have read. That is evidence against the design assumption that justified choosing the diagram over an oversized name. Also found the article dead end and the Nucli8 ownership gap, corroborated both outbound links, and named the team-attribution sentence as the reason it believed the H100 number. Original: Classmate reviewer | Read as a newcomer with a 60-second skim, then inspect links/articles. Dependency: P03. Acceptance: what was understood, one specific strength and change, usability findings; clearly simulated and not counted as real classmate comments or human reader test.
- [x] P04 | Coordinator | Triaged all four reports. Dependency: R01-R04. Output: reports/review-disposition.md. Nothing rejected outright; one item closed as already decided (R02-04, the H100 clearance question, settled at P01 and not re-asked), one folded (R02-03). Two convergences drove the triage. First, four blind reviewers hit one root cause from four directions: an article page carries no ownership context and no way out (R02-05, R04-03, R04-02, R01-05), handled as one fix pass rather than four patches. Second, three reviewers landed on the `32 H100s to 4` sentence and DISAGREED: two called the attribution too blanket to credit, and the newcomer named that same hedge as the reason it believed the number. That is a factual gap about which system Evan worked on, not a wording problem, so it went to Evan rather than to the writer. Recorded limitation: three of four reviewers never saw the site rendered, so this round is a content, claim and structure review and is NOT audience sign-off on the design. Original: Acceptance: each finding accepted/rejected/deferred/needs Evan with rationale and owner; actual required fixes become named checkbox tasks below. Review completion does not imply site approval.
- [ ] F01 | Writer | Resolve accepted claim/copy findings. Dependency: P04. Acceptance: each assigned finding mapped to a correction/evidence or documented no-op if none; renewed byline review only if approved text materially changes.
- [ ] F02 | Design | Resolve accepted layout/visual findings into a concrete handoff. Dependency: P04. Acceptance: each assigned finding mapped to a specific correction/check or documented no-op; no unrelated redesign.
- [ ] F03 | Coding | Implement accepted corrections and targeted technical rechecks. Dependency: P04, F01, F02. Acceptance: fixes mapped to findings, integrated approved copy, updated candidate/evidence, no-op recorded if none.
- [ ] R05 | Affected reviewers, coordinated separately | Recheck required findings against updated candidate. Dependency: F03. Acceptance: each affected reviewer gets its own report task/round; resolutions evidenced, unresolved items remain visible. No endless full-review loop for unchanged pages.
- [ ] H02 | Evan, coordinated by design | Run the actual unfamiliar-reader check. Dependency: P03; repeat only if material comprehension changes justify it. Acceptance: real person's answers after 60 seconds to what Evan builds, what connects the companies, and why contact him. Record answers honestly; resolve substantial confusion before launch.

## Deploy and course submission

- [ ] P05 | Coordinator | Confirm publication is authorized and candidate ready. Dependency: R05, H02 and required fixes. Acceptance: accepted release revision, clear authorization, no unresolved blockers; prior deployment authorization need not be re-requested.
- [ ] C06 | Coding | Build and deploy out/ through GitHub Pages Actions, then verify live pages and article assets. Dependency: P05. Acceptance: current content fetched and viewed at actual public URL, all three articles accessible, correct basePath and .nojekyll present, evidence tied to release; source tree and private material excluded. Deployment success alone does not pass.
- [ ] C07 | Coding with Evan for browser capture if needed | Create required verification artifacts. Dependency: C06. Acceptance: verification/ contains exactly screenshot.png with live URL bar, fetch.txt from actual URL, and three-line README.md with URL/time/falsifiable failure condition. No fabricated images or checks.
- [ ] P06 | Coordinator with Evan | Finalize decision-log drafts from real choices and checks. Dependency: C07 and relevant reports. Acceptance: Q1/Q2/Q4/Q5 reviewed by Evan, Q4 references real verification, Q5 states a real limitation, no report dump or invented history.
- [ ] H03 | Evan | Write DECISIONS.md Q3 in his own words. Dependency: candidate-moment notes available. Acceptance: actual overruling story or honest absence; agents may collect facts but cannot write the final answer.
- [ ] H04 | Evan, writer may prepare factual outline | Record 3-5 minute video. Dependency: C06. Acceptance: live tour, design choices, real challenge, duration/playback checked. No agent-generated substitute for Evan's account.
- [ ] H05 | Evan | Visit and comment on three actual classmates' sites in Canvas. Dependency: classmates' sites available. Acceptance: each has a specific strength and proposed change; actual posted comments confirmed. Agents do not send comments without explicit authorization.
- [ ] R06 | Teacher reviewer | Check final submission package. Dependency: C07, P06, H03-H05. Acceptance: artifacts and remaining submission steps checked against README, unknown Canvas state labeled unknown. Simulated assessment only.
- [ ] H06 | Evan, coordinator tracks evidence | Submit/confirm all five deliverables in Canvas. Dependency: R06 and any corrections. Acceptance: actual submission confirmation, not agent inference. Canvas remains the deadline authority.
- [ ] P07 | Coordinator | Close launch work and record remaining backlog. Dependency: H06. Acceptance: board reflects actual evidence, reports linked, plan status updated, no unchecked required task described as complete.

## Added fix tasks

Coordinator appends concrete findings here using IDs such as FIX-001, owner coding/design/writer, source finding ID, dependencies, acceptance check, and report link. Leave this section empty until actual findings exist. Do not invent defects in advance.

- [x] FIX-001 | Coding / run 02 | Fixed and verified twice. WritingList took a headingLevel prop with the h3 type scale held by a class, so /writing/ is now h1, h2, h2, h2 with zero skips and the homepage is unchanged. Item-heading type is byte-identical on both pages at 1440/640/375. The agent proved the check falsifiable by demoting the headings back to h3 in the hydrated DOM, which makes axe report 1 heading-order violation; with the fix, 0. Coordinator re-ran its own DOM walk and confirms no skip. Original finding: | Severity: important | `/writing/` skips from h1 to h3 because WritingList hardcodes h3 and the index has no h2 above it. Fix by giving WritingList a heading-level prop and holding the h3 type scale with a class so nothing moves visually. Dependency: C03, C04. Acceptance: a DOM walk of /writing/ reports no heading-level skip, and the computed font-size of the item headings is unchanged on both /writing/ and the homepage.
- [x] FIX-003 | Fixed in F03. Both decks restored to `AI + business systems` and `Probability + market data`, verified in the rendered export over HTTP rather than in source. Original: | Coding | Source: reports/W03-writer-02.md finding W-13 | Severity: important | The company deck lines render "AI and business systems" and "Probability and market data". The approved copy blocks say "AI + business systems" and "Probability + market data". The coordinator changed the separator during implementation without routing it, which is an unapproved edit to approved copy. Dependency: C03. Acceptance: both decks match the copy package character for character.
- [x] FIX-004 | Fixed in F03. The /writing/ description is now first person and matches the approved homepage Writing intro. Original: | Coding | Source: reports/W03-writer-02.md finding W-15 | Severity: suggestion | The /writing/ meta description is written in the third person ("Evan Baker writes about...") while every other surface on the site is first person. Coordinator-written, never writer-reviewed. Dependency: C03. Acceptance: the description is first person and matches the section intro already approved for the homepage.
- [ ] FIX-006 | Writer | Source: reports/C05-coding-02.md finding C05-05 | Severity: suggestion | The App Store listing titles itself "Edgebet App" while the site writes "EdgeBet" throughout. Decide which is correct and whether the site should match the store, then either leave it or route a one-word copy change. Needs Evan if the store listing itself is the thing that is wrong. Dependency: C05. Acceptance: the site and the store agree, or the difference is a recorded deliberate choice.
- [x] FIX-007 | Fixed in F03. The 404 page carries its own description. Original: | Coding | Source: reports/C05-coding-02.md finding C05-06 | Severity: suggestion | The 404 page inherits the site-wide meta description, which describes the homepage rather than a missing page. Dependency: C03. Acceptance: /404 carries its own description.
- [x] FIX-005 | Coding / run 03 (F03) | Done. The figcaption is deleted: zero hits for the string and zero figcaption elements in out/. Coding reports the band reads as a complete object at 1440, 688 and 375, the drawing sitting optically centred because band-pad is symmetric. The `.caption` CSS rule was deliberately kept so a line Evan writes later inherits the design-05 width cap. | Source: reports/W03-writer-02.md finding W-12 | Severity: blocker for publication | **RESOLVED BY THE COORDINATOR: delete the caption, do not replace it.** Design ruled on it as a layout element only and explicitly returned the content question to the writer, whose ruling therefore governs. The sentence was coordinator-written and states a universal claim about Evan's own method that no context file supports, so under the CLAUDE.md credibility rules it cannot stay and no agent may write its replacement. The `.caption` CSS rule stays in place so a line Evan writes later inherits design-05's width cap. Original finding: The writer's verdict is to delete the band `<figcaption>` outright: it states the thesis a second time above the company blocks when the copy package puts it exactly once in About, "Every company I build" is a universal claim about Evan's own method that no context file supports, and it misdescribes its own diagram (the middle two stations are `model` and `software`, so "the math in the middle" names neither). The writer deliberately did not draft a replacement, on the grounds that a different agent-written sentence keeps the defect and hides it behind better phrasing. Design was independently asked to rule on the same element as a layout question, and its spec calls for a caption under the diagram. Do not act until both rulings are in. Dependency: W03, D02. Acceptance: either the figcaption is gone and the band still reads as balanced at 1440/640/375, or a caption Evan wrote is in place.
- [x] FIX-002 | Coding / run 02 | Fixed and verified twice. .skip now uses the clip-path visually-hidden pattern with position: fixed on focus. The hidden box is [0,0,1,1] and still in the accessibility tree; driven Tab presses show it visible at [8,8,166,46] with the navy ring. position: fixed earns itself: on an article scrolled to y=1200 the focused link still lands at [8,8] rather than off-screen. Coordinator re-ran the overflow audit and it now returns an empty list at all three widths. Original finding: | Severity: suggestion | The skip link parks at `left: -9999px`, so its box sits far outside the viewport at every width. It does not cause horizontal scroll (verified), but it trips every honest overflow audit. Fix with the clip-path visually-hidden pattern plus `position: fixed` on focus. Dependency: C03. Acceptance: the overflow audit returns an empty list at 375/640/1440, and the link is still visible and focusable on the first Tab press.

- [x] design-05 | Coding / run 03 (F03) | Fixed and measured at all nine required widths. Evidence: reports/F03-coding-03.md. Rendered label size (specified size x viewBox scale) is now 17.00px on the vertical variant at 375/480/600/687 and 15.00 to 15.94px on the horizontal variant at 688/767/768/1024/1440, with stroke scale 1.000 to 1.063 everywhere. 9 of 9 on both criteria. The 200%-zoom worst case now reads 17.00px at stroke 1.000, up from 13.88px at 0.925. The report also prints getComputedStyle alongside, which reads a flat 15px at every width and would have passed the broken state, which is the point. Original finding: | Severity: important | The method line's station labels do not hold a size, because SVG text scales with its viewBox: 13.88px at a 640px viewport (which is also the 200%-zoom-at-1280 condition) against a 15px caption below them, and 20.3px at 639px, a 32% jump across a 1px viewport change. Hairlines land at 0.925px. This is the site's one bold move, so imprecision here costs the most. Fix: move the diagram query to 688px (where the column is exactly 640px and the scale is exactly 1.0) and cap `.method-v` at 335px. Acceptance: at 375, 480, 600, 687, 688, 767, 768, 1024 and 1440 the RENDERED label size (specified size multiplied by the viewBox scale) is 15.0 to 17.0 inclusive and the stroke scale is at least 1.0. Note that `getComputedStyle` reports 15px at every width and passes regardless, so it is not a valid check.
- [x] design-06 | Writer / run 03 (F01) | Fixed. Evidence: reports/F01-writer-03.md. The four lines are now `## ` headings in BOTH the private draft and the tracked source, with zero words changed. Byte-identity preserved and verified by hash before and after: the edited pair hashes f09ba877bf40 on both sides, 19 blocks each, and the two untouched pairs hold their pre-edit hashes. The edit was applied by a script holding the four strings in one shared list and asserting each occurs exactly once per file, so divergence between the two files was structurally impossible rather than checked afterward. Copy ruling, which was genuinely the writer's: no word changed and the numbering stays. Heading 1 survives at 46 characters because "Not 'shouldn't.' Can't." is the turn the argument is built on; the numbers stay because the intro promises a list and the ABSENCE of a number on the fourth is what signals the mechanism list ended and the scope caveat began. Remaining measured checks (28px at 1440, 64/16 gaps, no wrap past two lines at 375) belong to coding's recheck. Original finding: | Severity: important | Four lines in `draft-anything-send-nothing` render as bold paragraphs with equal 24px above and below, so the longest article's document outline is one h1 and nothing else. Visual bold standing in for a heading is a WCAG 1.3.1 failure, and zero h2 elements exist across all three articles. Fix: four `**...**` to `## ...` in BOTH the private draft and the tracked source, preserving the byte-identity property W03 verified. No CSS change; `.prose h2` already exists and is correct. Acceptance: a DOM walk reports h1 then four h2 with no skip, computed h2 is 28px at 1440, gaps are 64px above and 16px below, and no h2 wraps past two lines at 375.
- [x] design-07 | Coordinator | Source: reports/D02-design-02.md | Severity: suggestion | `/writing/` is reachable only by URL truncation; nothing on the site links to it. **Decision: keep it deliberately unlinked, recorded here and in website-plan.md.** It exists so a reader who truncates an article URL lands on something real instead of a 404. Design recommended against linking it and specifically against a "view all" row: three items already fully listed on the homepage do not need a link pointing at the same three items, and that gesture is on the spec's rejected list. Accepted as recorded.
- [x] design-08 | Coding / run 03 (F03) | Closed with no change, on measurement rather than assumption. next/font emitted a size-adjusted `Newsreader Fallback` (local Times New Roman, size-adjust 105.48%). Forced to that fallback at 375px the h3 holds one line with 18.2px of slack and identical height, so there is no reflow on swap, not merely no overflow. The writer separately proposed `Fair-price engine for betting markets` on copy grounds (parallel with the other three work titles, which take no article). That stands as an optional copy call for Evan, not a defect. Labelled inference recorded by coding: on a system with no Times New Roman the adjusted fallback never loads and the heading would settle two lines to one on swap; impossible on macOS and Windows. Original finding: | Severity: suggestion | At 375 the h3 "A fair-price engine for betting markets" clears its column by 0.8px, held on one line by `text-wrap: balance`. The risk is the Newsreader fallback reflowing it during the `display: swap` window. Acceptance: with the webfont blocked at 375, the text right edge stays inside the column. If it reflows, the fix is a one-word shorter title, which is a writer call.
- [ ] design-09 | Coding / run 03 (F03) | Source: reports/D02-design-02.md | Severity: suggestion | Stacked tap targets below 480px measure 42px, which passes WCAG 2.2 AA (24x24) but misses the 44px platform convention on the site's only navigation. Fix: `min-height: 44px` with flex centering below 480px, recorded as a third documented exception to the 8px scale alongside the 3px focus offset and the 0.18em underline offset. Acceptance: at 375 every nav and Contact link box is at least 44px tall and the stacked pitch stays on the 8px scale.

- [x] W-20 | Coordinator | Source: reports/F01-writer-03.md | Severity: process, important | Twice now a parallel rebuild has moved `out/` underneath an agent mid-verification: W03 hit it at 16:45:31 and F01 hit it at 17:04:34, five seconds after its own source edit. F01 nearly reported a wrong result, having started to treat a post-edit export as a pre-edit baseline, which would have compared its change against a render of its change. It caught the problem on file mtimes and fell back to proof that actually holds: the four pre-edit heading strings quoted verbatim in the D02 report. **Cause is coordinator scheduling, not agent error:** review and build tasks were dispatched against one shared artifact. **Correction, now binding:** no build runs while a review or verification task is open against the same artifact. AGENT-WORKFLOW.md step 5 already requires freezing site writes during the audience-review round; the same rule applies to builder verification. P03 freezes the candidate before R01-R04 dispatch.

- [ ] F03-01 | Coding / run 03 | Source: reports/F03-coding-03.md | Severity: suggestion | Design specified `display: flex` literally for the 44px tap target, which makes each anchor block-level, so at 375 the box is 335x44 and the focus ring spans the full column. **Coordinator ruling: use `inline-flex`.** Design's acceptance criterion is the 44px minimum, not the box model, and a full-column focus ring on a text link is inconsistent with every other link on the site. inline-flex measures 137x44, 113.3x44 and 62.2x44, still at or above 44 in both dimensions. Design may override at its recheck. Dependency: design-09. Acceptance: at 375/414/479 every nav and Contact link is at least 44px tall and the focus ring hugs the text.
- [ ] F03-02 | Coding, at deployment time | Source: reports/F03-coding-03.md | Severity: suggestion | The stale `out/404.html` premise did not reproduce: the file is byte-identical to `out/404/index.html` and root-relative in both a clean and an inherited build. The real effect is that `serve` 301-redirects `/404.html` to `/404`, so the local preview resolves the directory form and never exercises the file GitHub Pages actually serves. Converted from a fix into a deployment-time check. Dependency: C06. Acceptance: on the live Pages site, a URL that does not exist returns the styled 404 with working asset paths.

- [x] F01b | Writer / run 04 | Evidence: reports/F01b-writer-04.md. Ownership context added to two articles ("EdgeBet, my iOS app" and "Nucli8, is mine, built but not launched") in BOTH the private draft and the tracked source, with byte-identity re-verified 3 of 3; the coordinator independently re-hashed all three pairs under its own strip and all three match. **The writer overruled a reviewer and was right:** R02 listed "The 30x bug" as already clear because it says "built but not launched", but that is a status, not an owner, which is exactly R04-02's complaint. It matters more there than in the EdgeBet piece because the article publishes a billing-formula bug and a ten-defect audit for a named product. The third article genuinely passes and was left alone, recorded as a visible check rather than a silent one. **It also refused to ship R01-03 as worded**, correctly identifying that "every system Evan builds is structurally prevented from acting alone" repeats both defects that killed the band caption: a universal claim about Evan's method that no source supports, and a flattening of two different mechanisms (Demarly, Nucli8 and the employed work have a human gate; EdgeBet has no gate at all, it has a self-distrust rule). Its line: all four facts are true and the summary of them can still be false. Three scoped options drafted for Evan, placed outside a fenced block so coding cannot copy them by convention. The investor's $24.99/month observation logged as CUT with the reason attached so a later round does not reinstate it. Original: | Source: reports/review-disposition.md | Accepted copy pass: article and homepage ownership context (R02-05, R04-02), the Demarly status line (R01-02, availability only, no pricing), and a DRAFT ONLY of the sharper thesis (R01-03). Explicitly barred from the H100 attribution, the employment-timeline contradiction, and the band caption. Acceptance: a reader landing on any article page can tell whose system it describes; Demarly carries a status line consistent with EdgeBet and Nucli8; the thesis options reach Evan as options rather than shipping.
- [x] D03 | Design / run 03 | Evidence: reports/D03-design-03.md, plus the accent-role amendment applied to design-spec.md sections 2 and 8. Ruling: article pages get one `<aside>` headed "More writing" listing the other two pieces, built from the existing writing-list item with the excerpt removed. **design-07 stands unchanged and /writing/ stays unlinked**, on the reasoning that the homepage does not need a pointer to the writing list because it IS the list, while an article page contains no list at all, and at three articles a link to the index would hand the reader a page whose first item is the piece they just finished. Measured the real cost of the dead end rather than restating it: the homepage Writing section starts 65% down at 1440 and 5.9 phone screens down at 375. Compared four variants as rendered images by injecting candidate markup into the frozen export rather than rebuilding, so the freeze held. Rejected a titles-only version (48px gaps float with nothing binding each pair) and a 17px version (reads as a footnote row). design-12 ruled adjacent rather than left implied: the hero nav stays at three items. Original: | Source: reports/review-disposition.md | Rule on whether article pages get an exit path (R04-03) using existing vocabulary, reconcile it explicitly with the design-07 decision that /writing/ stays unlinked, and apply the accent-role amendment to design-spec.md sections 2 and 8. Acceptance: a concrete implementation instruction for coding with element, wording, placement, tokens and responsive behavior, and the two records no longer disagreeing.
- [x] F03b | Coding / run 04 | Applied both agents' accepted outputs. Evidence: reports/F03b-coding-04.md. Article pages now carry a "More writing" aside listing the other two pieces; per-page enumeration is exactly 2 writing links and 1 aside on each of the three articles, 0 asides on the homepage, /writing/ and 404. Both writer strings verified over HTTP; zero hits for 24.99, 7-day or trial. Lighthouse accessibility re-measured at 100 with zero failing audits on the changed article page, plus axe-core best-practice rules on six pages at 0 violations, run specifically because landmark-complementary-is-top-level and heading-order are not in Lighthouse's default set and this change adds a landmark. Rendered geometry matched design's prediction to within 0.2px. Coordinator re-verified independently: link and aside counts, both strings over the served export, zero pricing hits, and zero heading skips, zero out-of-viewport elements and zero band accents at all six page/width combinations. **Three things the agent flagged rather than buried:** the 96px footer gap reads 98px link-rect to link-rect and 96px box to box (inline leading, both numbers reported); its own shasum comparison of writing/ vs content/articles/ printed DIFFER, which it correctly identified as a defect in its method (the pairs have different filenames and need normalizing) and recorded in the report so nobody re-derives it and panics; and a cross-module `:global` selector that wins on specificity today but would silently revert if WritingList ever raises its own, logged as backlog F03b-02. Original: | Apply the writer's exact homepage strings and design's article-exit instruction, then rerun the frozen-build check set. Acceptance: each change mapped to its finding ID with a measured result.

- [ ] design-13 | Evan | Source: reports/D03-design-03.md | Severity: observation, no fix proposed | A purely visual note that bears on R04's finding that the method line did not decode. The four station words render at 15/17px weight 500, **the same size as the hero nav links**, so within the page's own type scale the diagram's words carry no more weight than navigation. Everything making the band prominent is the black field, not the type. Design deliberately proposed no fix and wrote no words, because the caption slot is Evan's. Record only; pair this with the R04 first-impression evidence when deciding what, if anything, goes under the diagram.

- [ ] F03b-02 | Coding | Source: reports/F03b-coding-04.md | Severity: backlog | `.more :global(ul)` overrides WritingList's 48px list gap by specificity, (0,1,1) over (0,1,0). It measures the intended 32px and design permitted the escape hatch, but it is a cross-module reach that would silently revert to 48px if WritingList ever raises its own selector. Alternative recorded: a `listGap` prop. Not urgent; it is correct today.
- [ ] P05b | Coordinator | Re-run the affected reviewers against the corrected candidate (R05) once Evan's open inputs land. Dependency: Evan's answers on the timeline, the H100 attribution, the caption, and byline approval. Only the affected reviewers re-run, not a full second round: R02 for the timeline and attribution, R04 for the article exit and the diagram, R01 for the Demarly status line and any thesis change. Do not re-run reviewers whose findings were not touched.

## Homepage content refinement, 2026-09-22

Authorization: Evan asked to implement critique changes 1-3: a specific opening, less repeated copy, and stronger product decisions. Local implementation and verification authorized. Product screenshots will follow separately.

- [x] CP01 | Writer / content-refinement-01 | Complete. Evidence: context/homepage-refinement.md and reports/CP01-writer-01.md. Exact copy reviewed by coordinator; supported scope corrections for player-prop reruns and Nucli8 minimum-instance optimization accepted. Professional paragraph unchanged.
- [x] CP02 | Coding / content-integration-01 | Complete. Scope: components/Hero.tsx, components/About.tsx, app/page.tsx, app/layout.tsx and reports/CP02-coding-01.md. Copy integrated with existing styles and ledger article link. Typecheck/lint/export pass; desktop/mobile screenshots reviewed, no overflow at 1440/375/640, Lighthouse accessibility 100. 640px is a reflow proxy, not actual 200% browser zoom. Writer audited source parity and coordinator checked exported content and screenshots. Evidence: reports/CP03-coordinator-01.md and CP02 report. Static preview http://localhost:58254/; dev http://localhost:3100/.

## Post-launch backlog

## Product screenshots, 2026-09-22

Authorization: Evan supplied IMG_8969.PNG for EdgeBet and Screenshot 2026-09-22 at 9.31.26 AM.png for Demarly for the agreed homepage imagery addition. Local implementation authorized.

- [x] IM01 | Design / screenshot-01 | Complete. Evidence: reports/IM01-design-01.md. Coordinator accepted natural aspect ratios, Demarly full-column overview, EdgeBet 288px portrait beside copy on desktop and stacked on mobile, captions and full-size links.
- [ ] IM02 | Coding / screenshot-01 | In progress. Write scope app/page.tsx, components/CompanyEntry.tsx, components/CompanyEntry.module.css, public/images/, reports/IM02-coding-01.md. Implement approved screenshot presentation, descriptive alt and full-size image links with basePath-safe assets. Verify build, mobile/desktop rendering, image links and accessibility.

## Post-launch backlog (continued)

- [ ] B01 | Writer | Review remaining seven drafts after launch; Apple piece stays blocked until Evan verifies rejection reasons/order. No implied byline approval.
- [ ] B02 | Design + coding | Consider venture detail pages only if they add useful evidence and Evan authorizes the scope.
- [ ] B03 | Design + coding | Add Evan's approved photo if supplied; preserve a complete layout without it.
